# 📤 回覆 AI 聊天控制後台

---

## ✅ 配置完成！以下是測試信息

### 1️⃣ API 地址

```
http://165.154.254.99:8080
```

### 2️⃣ API Key

```
ai-chat-system-test-key
```
> 測試環境已配置為允許任意 Key，您可以使用任何字符串

### 3️⃣ 測試用戶

| 用戶類型 | Telegram ID | 餘額 | 狀態 |
|---------|-------------|------|------|
| 真實用戶 | `5433982810` | 待充值 | 需要在服務器執行設置腳本 |

### 4️⃣ AI 帳號

**⚠️ 請提供您的 AI 帳號 Telegram ID！**

您提到會從 Worker 節點獲取 AI 帳號，請告訴我：
- 實際的 Telegram ID 列表
- 每個帳號需要的初始餘額

---

## 🔧 服務器配置步驟（需要在 SSH 終端執行）

請在服務器上執行以下命令：

```bash
# 1. 重啟 API 服務（載入新的 AI API 路由）
sudo systemctl restart luckyred-api

# 2. 等待服務啟動
sleep 3

# 3. 測試 AI API 是否可用
curl http://localhost:8080/api/v2/ai/status

# 4. 運行測試帳號設置腳本
cd /opt/luckyred
python scripts/setup_ai_test_accounts.py
```

---

## 🧪 API 測試命令

### 測試 1：健康檢查

```bash
curl http://165.154.254.99:8080/api/v2/ai/status
```

**預期響應：**
```json
{
  "success": true,
  "data": {
    "status": "ok",
    "version": "2.0"
  }
}
```

### 測試 2：查詢餘額

```bash
curl -X GET "http://165.154.254.99:8080/api/v2/ai/wallet/balance" \
  -H "Authorization: Bearer ai-chat-system-test-key" \
  -H "X-Telegram-User-Id: 5433982810"
```

### 測試 3：發送紅包

```bash
curl -X POST "http://165.154.254.99:8080/api/v2/ai/packets/send" \
  -H "Authorization: Bearer ai-chat-system-test-key" \
  -H "X-Telegram-User-Id: 5433982810" \
  -H "Content-Type: application/json" \
  -d '{
    "currency": "usdt",
    "packet_type": "random",
    "total_amount": 5.0,
    "total_count": 3,
    "message": "測試紅包"
  }'
```

---

## 📋 待您提供的信息

1. **AI 帳號 Telegram ID 列表**
   ```json
   [
     {"tg_id": ???, "name": "AI Player 1"},
     {"tg_id": ???, "name": "AI Player 2"},
     ...
   ]
   ```

2. **每個 AI 帳號初始餘額需求**
   - 建議：50 USDT / 帳號

3. **測試場景優先級**
   - 先測哪個功能？

---

## 📞 聯繫

收到您的 AI 帳號列表後，我會：
1. 在數據庫中創建所有帳號
2. 為每個帳號充值初始餘額
3. 確認 API 服務正常運行
4. 通知您開始測試

**請回覆 AI 帳號的 Telegram ID！**
